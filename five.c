#include<stdio.h>
#include<string.h>
/* program to find length of strig */
int main()
{
   char a[40];
   printf("Enter the string");
   scanf("%s",a);
   printf("Length is %d\n",strlen(a));
return 0;
}